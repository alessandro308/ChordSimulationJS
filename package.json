{
  "name": "my-project",
  "version": "1.0.0",
  "scripts": {
    "build": "./node_modules/.bin/babel src -d lib"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1"
  }
}
